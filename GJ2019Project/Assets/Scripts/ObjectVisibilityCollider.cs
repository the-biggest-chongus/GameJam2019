using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectVisibilityCollider : MonoBehaviour
{
    //get list of all objects that should disappear when a room is entered



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void Disappear(GameObject exception){
        //exception = room that shouldn't be made invisible

        foreach()
    }
}
